// radius.ts
import { moderateScale } from 'react-native-size-matters';

export const radius = {
  sm: moderateScale(10),
  md: moderateScale(16),
  lg: moderateScale(24),
  xl: moderateScale(32),
  pill: 999,
};