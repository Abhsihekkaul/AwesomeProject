# HealingSathi — Branding Prompt

This is a ready-to-paste prompt for asking Claude (or any image-generating tool) to design
a logo, app icon, and splash screen for HealingSathi. Copy the whole block below into a new
conversation and attach/reference it as-is — it already contains everything about the app,
its personality, and its existing design system so the output matches what's already built.

Nothing here is generated yet — this is the *brief*, not the artwork. Once you have a result
you like, drop the exported files into `src/assets/branding/` and update the app icon /
splash screen following the steps in `layman.md`.

---

## Copy everything below this line into your prompt

